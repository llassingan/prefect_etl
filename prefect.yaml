# generic metadata
prefect-version: null
name: null

# preparation steps
build: null
push: null

# runtime steps
pull: null

# deployment configurations
deployments:
  - name: deployment-1
    version: null
    tags: ["dev"]
    description: null
    schedule: null
    entrypoint: script/flows/pg_to_pg_etl.py:greetings #na fungsi flow nya
    parameters:
        name: "andi"  #default parameter
    work_pool:
        name: default-agent-pool #workpool
        work_queue_name: default